{
  "extends": "../tsconfig.json",
  // Node binaries in this directory require commonjs module format,
  // which conflicts with the esnext format in the root directory
  // above.
  "compilerOptions": { "module": "commonjs" }
}
